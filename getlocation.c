#include "main.h"
/**
 * get_location - main function
 * @command: parameter
 * Return: full path or NULL
 */
char *get_location(char *command)
{
	char *location = NULL;
	char *path, *path_copy;

	path = getenv("PATH");

	return (location);
}

